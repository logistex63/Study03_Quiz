# 상식 퀴즈 웹 앱 PRD

- 작성일: 2026-09-18
- 상태: 사용자 검토 대기

## 1. 개요

| 항목 | 내용 |
|---|---|
| 누가 | 대학 1학년 학생이 혼자 상식을 공부하며 풂 |
| 무엇을 | 4지선다 상식 퀴즈 웹 앱. 카테고리 4개(한국사, 세계지리, 과학, 예술과 문화), 카테고리마다 10문제, 총 40문제 |
| 왜 | 틀린 문제를 그 자리에서 알고 넘어가도록. 답을 고르면 곧바로 정답 여부와 한 줄 해설을 보여 줌 |
| 언제 | 한 판은 카테고리 1개의 10문제. 10문제가 끝나면 점수를 보여 줌 |
| 어디서 | 브라우저에서 파일을 열면 동작함(`file://`). 서버와 빌드 도구는 없음 |
| 어떻게 | 3단계로 나눠 만듦(아래 6절) |

## 2. 파일 구성

파일은 다음 4개만 둡니다.

| 파일 | 역할 |
|---|---|
| `index.html` | 틀과 `<main id="app">`만 둠. `questions.js` → `script.js` 순서로 일반 `<script src>`로 불러옴(`type="module"`은 쓰지 않음. `file://`에서 막히기 때문) |
| `style.css` | 화면 배치, 정답과 오답 색, 흐려진 힌트 보기. 폭 360px에서도 가로 스크롤이 없어야 함 |
| `script.js` | `state`, `MODES`, `render()`, 자료 검사, 섞기, 타이머, 저장소 함수 |
| `questions.js` | 전역 상수 `QUESTIONS` 배열 하나 |

문항 자료는 JSON 파일을 `fetch`로 읽지 않고 `questions.js`의 전역 상수로 불러옵니다. `file://`에서는 `fetch`가 막히기 때문입니다.

## 3. 구조

- **상태 객체 하나**: `script.js`는 `state` 하나에 현재 화면, 카테고리, 모드, 이번 판의 문항 순서, 현재 문항 번호, 답 기록, 점수, 힌트 사용, 타이머 시작 시각을 둡니다.
- **그리기 함수**: 사용자가 무언가를 하면 `state`를 바꾸고 `render()`를 부릅니다. `render()`는 `state.screen`에 맞는 화면 함수(시작, 문제, 결과, 순위표)로 `#app`을 다시 그립니다.
- **모드 설정표**: 모드별 차이는 `MODES` 한 곳에 모읍니다. 이벤트 처리 코드에서 모드 이름으로 분기하지 않고 설정값(`timeLimit`, `hint`, `ranked` 등)을 읽습니다.

```js
const MODES = {
  practice: { label: "연습",   timeLimit: null, hint: false, ranked: false },
  speed:    { label: "스피드", timeLimit: 15,   hint: false, ranked: true  },
  hint:     { label: "힌트",   timeLimit: null, hint: true,  ranked: true  },
};
```

## 4. 화면 흐름

1. **시작 화면**: 카테고리 4개 중 하나와 모드를 고르고 [시작]을 누릅니다. 1단계에는 모드 선택이 없습니다. 3단계부터는 [순위표] 버튼이 있습니다.
2. **문제 화면**: "3 / 10"처럼 진행 상황, 문제, 보기 4개를 보여 줍니다. 스피드 모드는 남은 초를, 힌트 모드는 [힌트] 버튼을 함께 보여 줍니다.
3. **답을 고른 뒤**:
   - 보기 4개가 모두 잠깁니다.
   - 정답 보기는 초록색과 ✔, 고른 오답은 빨간색과 ✘로 표시합니다. 색만으로 정오를 구분하지 않습니다.
   - "정답입니다" 또는 "오답입니다"(스피드 모드의 시간 초과는 "시간 초과"), 한 줄 해설, 출처 링크를 보여 줍니다.
   - [다음]을 눌러야 다음 문항으로 넘어갑니다. 자동으로 넘기지 않습니다. 마지막 문항에서는 버튼이 [결과 보기]가 됩니다.
4. **결과 화면**:
   - 점수를 "7.5 / 10"처럼 보여 줍니다. 힌트 모드는 힌트를 쓴 횟수도 보여 줍니다.
   - 틀린 문항 목록(문제, 정답, 해설)을 보여 줍니다.
   - [다시 하기](같은 카테고리와 모드로 새로 섞어서 시작)와 [처음으로] 버튼이 있습니다.
   - 연습 모드이고 틀린 문항이 있으면 [틀린 문제 다시 풀기]가 있습니다.
   - 3단계부터는 새 기록이 상위 5개에 들면 "새 기록! 2위"처럼 알려 줍니다.
5. **순위표 화면**(3단계): 7절 참고.

문제 화면에서 [처음으로]를 누르면 그 판을 버리고 시작 화면으로 돌아갑니다. 버린 판은 저장하지 않습니다.

## 5. 문항 자료

### 5.1 형식

```js
const QUESTIONS = [
  {
    id: "kh-01",                 // 카테고리 접두어 + 번호
    category: "한국사",          // 한국사 | 세계지리 | 과학 | 예술과 문화
    difficulty: "보통",          // 쉬움 | 보통 | 어려움
    question: "…",
    choices: ["…", "…", "…", "…"],
    answer: "…",                 // choices 중 하나와 글자가 똑같아야 함
    explanation: "…",            // 한 줄 해설
    source: { title: "…", url: "…" }
  },
  // … 40개
];
```

id 접두어는 한국사 `kh`, 세계지리 `wg`, 과학 `sc`, 예술과 문화 `ac`입니다.

정답은 위치 번호가 아니라 보기 글자로 적습니다. 보기를 섞은 뒤 화면에서 정답 위치를 다시 계산합니다.

### 5.2 섞기

매 판 시작할 때 그 카테고리의 문항을 섞어(Fisher–Yates) 앞에서 10개를 뽑고, 각 문항의 보기 4개 순서도 섞습니다. 원본 `QUESTIONS`는 바꾸지 않습니다.

### 5.3 자료 검사

앱을 시작할 때 `QUESTIONS`를 검사합니다. 오류가 있으면 시작 화면 대신 오류 목록을 화면에 보여 주고 콘솔에도 출력합니다.

- 카테고리가 4개 중 하나이고, 카테고리마다 10문항 이상
- 난이도가 쉬움, 보통, 어려움 가운데 하나
- `id`가 겹치지 않음
- `choices`가 4개이고, 서로 겹치지 않고, 빈 문자열이 없음
- `answer`가 `choices` 안에 정확히 한 번 있음
- `question`, `explanation`, `source.title`, `source.url`이 비어 있지 않음

### 5.4 문항 규칙

1. **정답은 하나만**: 다른 보기 3개는 분명히 틀린 답이어야 합니다. "모두 정답", "정답 없음", "위의 것 모두" 같은 보기는 쓰지 않습니다.
2. **기준과 시점 적기**: 「가장 ~한」, 「최초」, 순위, 수치처럼 기준이나 시점에 따라 답이 달라지는 문항은 기준과 시점을 문제에 적습니다. 예: "2024년 UN 통계 기준, 면적이 가장 넓은 나라는?"
3. **해설과 출처**: 해설은 한 줄로 씁니다. `source`에는 실제로 열어서 확인한 출처의 이름과 URL을 적습니다. 백과사전, 공공기관, 학술 기관 자료를 우선합니다.

### 5.5 작성과 검수 절차

1. Claude가 문항을 씁니다. 문항마다 웹에서 출처를 열어 정답과 해설 내용을 확인합니다.
2. 출처로 확인되지 않거나 정답이 둘 이상일 여지가 있는 문항은 버리고 새로 씁니다.
3. 40문항을 다 쓰면 카테고리, 문제, 정답, 출처를 표로 정리해 사용자에게 검토를 받습니다.
4. 사용자가 고치라고 한 문항을 반영한 뒤 `questions.js`를 확정합니다.

## 6. 단계별 범위

### 1단계: 기본 퀴즈와 점수

- 시작 화면에서 카테고리를 고르고 10문제를 풉니다.
- 모드 선택은 없습니다. 시간 제한과 힌트가 없고, 맞히면 1점입니다.
- 답을 고르면 즉시 정답 여부, 해설, 출처를 보여 줍니다(4절 3번).
- 10문제가 끝나면 결과 화면에 점수와 틀린 문항을 보여 줍니다.
- 40문항과 자료 검사가 모두 들어갑니다.

### 2단계: 게임 모드 3개

시작 화면에 모드 선택이 생기고, 1단계의 기본 퀴즈는 아래 모드 3개로 대체됩니다. 힌트를 한 번도 쓰지 않은 힌트 모드가 1단계의 기본 퀴즈와 같습니다.

| 모드 | 시간 제한 | 힌트 | 맞힌 문제 점수 | 순위표 기록(3단계) |
|---|---|---|---|---|
| 연습 | 없음 | 없음 | 1점(결과 화면에만 보여 줌) | 안 함 |
| 스피드 | 문항당 15초 | 없음 | 1점 | 함 |
| 힌트 | 없음 | 문항당 1번 | 1점, 힌트를 썼으면 0.5점 | 함 |

틀린 문항은 모든 모드에서 0점입니다.

**연습 모드**
- 점수는 결과 화면에 보여 주기만 하고 순위표에는 올리지 않습니다.
- 결과 화면의 [틀린 문제 다시 풀기]를 누르면 이번 판에서 틀린 문항만 다시 섞어서 풉니다. 문항 수는 틀린 개수와 같습니다.
- 다시 푼 판에서도 틀린 문항이 있으면 [틀린 문제 다시 풀기]를 또 할 수 있습니다. 모두 맞히면 버튼이 사라집니다.

**스피드 모드**
- 문항이 나타나면 15초에서 거꾸로 세어 화면에 보여 줍니다.
- 남은 시간은 매번 `타이머 시작 시각`과 현재 시각의 차이로 계산합니다. 1초씩 빼는 방식은 쓰지 않습니다. 그래야 탭을 잠시 떠났다 와도 어긋나지 않습니다.
- 0초가 되면 오답으로 처리하고 "시간 초과"와 함께 정답, 해설, 출처를 보여 줍니다.
- 답을 고르거나 시간이 다 되면 타이머가 멈춥니다. 해설을 읽는 동안에는 시간이 흐르지 않습니다.
- 판을 떠나면(결과 화면, 처음으로) 타이머를 반드시 정리합니다.

**힌트 모드**
- [힌트]를 누르면 오답 보기 3개 중 2개를 무작위로 골라 흐리게 하고 누를 수 없게 합니다.
- 한 문항에 한 번만 쓸 수 있고, 쓴 뒤에는 버튼이 꺼집니다. 답을 고른 뒤에도 꺼집니다.
- 힌트를 쓰고 맞히면 0.5점입니다.

### 3단계: 점수 저장과 순위표

7절을 모두 구현합니다.

## 7. 점수 저장과 순위표 (3단계)

### 7.1 저장

- localStorage 키 `quiz.scores` 하나에 JSON 배열로 저장합니다.
- 기록 1건: `{ category, mode, score, hintsUsed, date }`
  - `mode`는 `"speed"` 또는 `"hint"`
  - `hintsUsed`는 스피드 모드에서 0
  - `date`는 ISO 형식 문자열(`new Date().toISOString()`). 화면에는 "2026-09-18"처럼 날짜만 보여 줍니다.
- 스피드 모드나 힌트 모드에서 10문제를 모두 마쳤을 때 한 번 저장합니다. 연습 모드와 중간에 그만둔 판은 저장하지 않습니다.
- 저장할 때 카테고리와 모드 조합마다 상위 5개만 남기고 나머지는 버립니다.

### 7.2 순서

같은 카테고리와 모드 안에서 다음 순서로 정렬합니다.

1. 점수가 높은 순
2. 힌트를 덜 쓴 순(힌트 모드)
3. 먼저 세운 기록 순(`date`가 이른 순)

### 7.3 순위표 화면

- 카테고리 4개 × 점수 모드 2개(스피드, 힌트) = 표 8개입니다.
- 위쪽 탭에서 카테고리를 고르면 그 카테고리의 스피드 표와 힌트 표가 나란히 나옵니다. 좁은 화면에서는 위아래로 나옵니다.
- 각 표는 순위, 점수, 날짜를 최대 5행까지 보여 줍니다. 힌트 표에는 힌트 횟수 열이 더 있습니다. 기록이 없으면 "아직 기록이 없습니다"라고 보여 줍니다.
- [기록 모두 지우기] 버튼을 누르면 확인 창을 한 번 띄운 뒤 `quiz.scores`를 지웁니다.

### 7.4 오류 처리

- localStorage를 읽고 쓰는 모든 곳을 `try/catch`로 감쌉니다.
- 저장된 값이 JSON이 아니거나 배열이 아니면 빈 기록으로 봅니다. 형식이 맞지 않는 기록 1건은 건너뜁니다.
- 브라우저가 저장을 막으면(시크릿 창, 저장소 차단 등) 퀴즈는 정상으로 돌아가고, 순위표와 결과 화면에만 "기록을 저장할 수 없습니다"를 보여 줍니다.

## 8. 확인 방법 (단계별 완료 기준)

서버와 빌드 도구 없이 `index.html`을 브라우저에서 직접 열어 확인합니다. 각 단계에서 브라우저 도구로 실제로 눌러 보고 결과를 보고합니다.

**공통**
- 앱을 시작할 때 자료 검사 오류가 0건이어야 합니다.
- 콘솔에 오류가 없어야 합니다.
- 폭 360px에서 가로 스크롤이 없어야 합니다.

**1단계**
- `index.html`을 더블클릭해서 열면 시작 화면이 나옵니다.
- 카테고리 4개를 각각 끝까지 풀었을 때 10문제가 나오고, 한 판 안에서 같은 문항이 두 번 나오지 않습니다.
- 답을 고르면 즉시 정답 여부, 해설, 출처가 나오고 보기가 잠깁니다.
- 결과 화면의 점수가 맞힌 개수와 같습니다.
- 다시 하기를 하면 문항과 보기 순서가 바뀝니다.

**2단계**
- 스피드 모드에서 가만히 두면 15초 뒤에 시간 초과로 오답 처리되고, 해설을 읽는 동안 시간이 흐르지 않습니다.
- 힌트 모드에서 힌트를 쓰면 오답 2개가 흐려지고 누를 수 없습니다. 맞히면 0.5점이 됩니다. 한 문항에서 두 번 쓸 수 없습니다.
- 연습 모드에서 [틀린 문제 다시 풀기]를 하면 틀린 문항만 나옵니다.

**3단계**
- 스피드와 힌트 모드의 기록이 새로고침한 뒤에도 순위표에 남아 있습니다.
- 연습 모드와 중간에 그만둔 판은 기록되지 않습니다.
- 6판 이상 하면 표마다 5개로 잘리고, 7.2의 순서대로 정렬됩니다.
- [기록 모두 지우기]가 동작합니다.
- 시크릿 창이나 저장소를 막은 상태에서도 퀴즈를 끝까지 풀 수 있습니다.

## 9. 하지 않는 것

회원가입과 로그인, 서버, 여러 사람이 공유하는 순위표, 이름 입력, 문항 편집 화면, 다국어, 효과음과 애니메이션 연출은 만들지 않습니다.
